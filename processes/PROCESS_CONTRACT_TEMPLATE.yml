process_id: example.v1
title: Example Process
status: active
kind: process
version: 1
owner: lab
process_class: effect
organ: law
wakes: []
requires_infra: []
composable: false
requires_target_hash: false
idempotency: none
evidence_required: false
activation_ritual:
  slots:
    who: {meaning: "autoridade solicitante", source: session, predicate: who.authorized}
    did: {meaning: "ato admitido", source: llm, predicate: did.allowed, values: [example_act]}
    this: {meaning: "alvo canônico", source: llm, predicate: this.canonical}
    when: {meaning: "instante de registro", source: clock, predicate: when.registered_at}
    confirmed_by: {meaning: "confirmação do rito", source: session, predicate: confirmed_by.authority}
    if_ok: {meaning: "continuidade de sucesso", source: contract, predicate: if_ok.compatible}
    if_doubt: {meaning: "continuidade de dúvida", source: contract, predicate: if_doubt.compatible}
    if_not: {meaning: "continuidade negativa", source: contract, predicate: if_not.compatible}
    status: {meaning: "estado inicial", source: contract, predicate: status.initial}
  required_aux: []
  optional_aux: []
authority_scope:
  allowed_who: []
  required_grants: []
adapter:
  name: ""
  danger_tier: L0
budget_policy:
  acu_limit: 0
  timeout_seconds: 0
  rate_limit: none
evidence_obligation:
  required: false
  must_include: []
closure_shape:
  success_status: fechado
  doubt_status: doubted
  ghost_status: ghost
if_doubt:
  behavior: attention_raise
runtime_readiness:
  checks: []
doubt_path: attention-raise.v1
