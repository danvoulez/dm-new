process_id: attention-raise.v1
organ: law
activation_ritual:
  slots:
    who: {meaning: "autoridade que pede atenção", source: session, predicate: who.authorized}
    did: {meaning: "ato de atenção admitido", source: llm, predicate: did.allowed, values: [raise_attention]}
    this: {meaning: "objeto que requer atenção", source: llm, predicate: this.canonical}
    when: {meaning: "instante de registro do pedido", source: clock, predicate: when.registered_at}
    confirmed_by: {meaning: "autoridade que confirma o pedido", source: session, predicate: confirmed_by.authority}
    if_ok: {meaning: "continuidade após acolhimento", source: contract, predicate: if_ok.compatible, values: [attention-raise.v1]}
    if_doubt: {meaning: "continuidade se a atenção não puder ser resolvida", source: contract, predicate: if_doubt.compatible, values: [attention-raise.v1]}
    if_not: {meaning: "continuidade negativa", source: contract, predicate: if_not.compatible, values: [stop]}
    status: {meaning: "estado inicial do pedido", source: contract, predicate: status.initial, values: [registered]}
  required_aux: []
  optional_aux: []
authority_scope: []
adapters: []
evidence_obligation: separate-result-act
doubt_path: attention-raise.v1
